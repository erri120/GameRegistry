---
extends: 'default'

rules:
    line-length: 'disable'

    brackets:
        forbid: false
        min-spaces-inside: 1
        max-spaces-inside: 1
        min-spaces-inside-empty: 0
        max-spaces-inside-empty: 0

    quoted-strings:
        quote-type: 'single'
        required: true
